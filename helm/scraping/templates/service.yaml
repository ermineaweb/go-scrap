apiVersion: v1
kind: Service
metadata:
  name: {{ include "scraping.fullname" . }}
  labels:
    {{- include "scraping.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scraping.selectorLabels" . | nindent 4 }}
